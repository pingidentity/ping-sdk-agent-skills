/*
 * 
 *
 * App.swift.template
 *
 * Copyright (c) 2026 Ping Identity Corporation. All rights reserved.
 * This software may be modified and distributed under the terms
 * of the MIT license. See the LICENSE file for details.
 */

/*
 * App Entry Point — @main struct for the SwiftUI application.
 *
 * INSTRUCTIONS:
 *   1. Rename to match your app (e.g., MyApp.swift)
 *   2. Register URL scheme handlers for OAuth callbacks if using external IdPs.
 *   3. Register optional callback modules in init() if needed.
 */

import SwiftUI
import PingJourney
import PingOrchestrate

// Uncomment these imports as needed for optional callback modules:
// import PingBinding
// import PingFido
// import PingExternalIdP
// import PingProtect
// import PingDeviceProfile
// import PingReCaptchaEnterprise

@main
struct MyApp: App {

    init() {
        // Register optional callback modules here.
        // Each module registers its callback types so the Journey
        // can recognize and create them from server responses.
        //
        // Uncomment the modules your Journey trees use:

        // PingBinding.BindingModule.register()
        // PingFido.FidoModule.register()
        // PingExternalIdP.ExternalIdPModule.register()
        // PingProtect.ProtectModule.register()
        // PingDeviceProfile.DeviceProfileModule.register()
        // PingReCaptchaEnterprise.ReCaptchaEnterpriseModule.register()
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
                .onOpenURL { url in
                    // Handle URL scheme callbacks (e.g., Google, Facebook OAuth redirects).
                    // The SDK typically handles this automatically, but you can add
                    // custom URL handling logic here if needed.
                }
        }
    }
}
