/*
 *
 *
 * build.gradle.kts.template
 *
 * Copyright (c) 2026 Ping Identity Corporation. All rights reserved.
 * This software may be modified and distributed under the terms
 * of the MIT license. See the LICENSE file for details.
 */

/*
 * App-level build.gradle.kts — DaVinci SDK dependencies for Ping Identity.
 *
 * INSTRUCTIONS:
 *   Merge the sections below into your existing app/build.gradle.kts.
 *   Replace <version> / <bom_version> with the latest release versions.
 */

plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("org.jetbrains.kotlin.plugin.compose")
}

android {
    namespace = "com.example.myapp"
    compileSdk = 36

    defaultConfig {
        applicationId = "com.example.myapp"
        minSdk = 28
        targetSdk = 36
        versionCode = 1
        versionName = "1.0"

        // Required for OAuth 2.0 redirect
        manifestPlaceholders["appRedirectUriScheme"] = "org.forgerock.demo"
    }

    buildFeatures {
        compose = true
    }
}

dependencies {
    // ─── Ping Orchestration SDK (DaVinci module) ────────────────────────────
    implementation("com.pingidentity.sdks:davinci:<version>")

    // Optional: Social login (Google, Facebook, Apple)
    // implementation("com.pingidentity.sdks:external-idp:<version>")

    // Optional: PingOne Protect risk signals
    // implementation("com.pingidentity.sdks:protect:<version>")

    // Optional: FIDO2 / WebAuthn passkey support
    // implementation("com.pingidentity.sdks:fido:<version>")

    // ─── Jetpack Compose ────────────────────────────────────────────────────
    val composeBom = platform("androidx.compose:compose-bom:<bom_version>")
    implementation(composeBom)
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.material:material-icons-extended")
    implementation("androidx.compose.foundation:foundation")
    implementation("androidx.activity:activity-compose")
    implementation("androidx.navigation:navigation-compose")
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose")

    // ─── DataStore (token persistence) ──────────────────────────────────────
    implementation("androidx.datastore:datastore-preferences")
}
